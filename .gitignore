# Local OCR outputs (large / regenerated)
/ocr/

# Local PDF/OCR tooling (Node scripts + node_modules)
/tools/
/tool/

# OS
Thumbs.db
.DS_Store

# Пайплайн генерации вопросов (content/runs)
# Машинозависимые payload импорта (id конкретной БД Damulab) — не коммитить.
.local/
# Журналы ролей прогона (logs/analyst.md и т.п.) — рабочие записки, истина в JSON 00–05.
content/runs/**/logs/

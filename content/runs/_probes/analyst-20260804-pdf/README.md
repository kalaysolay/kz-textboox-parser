# Проба аналитика: чтение страниц PDF (2026-08-04)

Дата: 2026-08-04. Старую папку `analyst-20260804/` **не перезаписывали**.

Цель — проверить новую постановку роли [damulab-textbook-analyst](../../../../.cursor/skills/damulab-textbook-analyst/SKILL.md):
формулировки берутся со **страниц PDF** (через `content/tools/read-textbook-pages.js`),
parsed JSON — только навигация и перекрёстная проверка.

Утилита для каждой темы положила PNG/TXT в `pages-* /`; срезы — `probe-*.slice.json`.

## Результат

| # | Тема | Стр. | Дословно с PDF | Было (старая проба / пилот) | `sliceStatus` |
|---|------|------|----------------|------------------------------|---------------|
| 1 | 4.5 Умножение десятичной дроби на натуральное число | 45–50 | правило в рамке p.45–46 + свойства умножения | `ready_unverified`, 2× `RC_*` | **ready** |
| 2 | 4.12 Числовые последовательности из дробей | 90–91 | упражнения 1005–1014 читаемы; формального «Правила» нет, опора — инструкции заданий + формула \(a_n\) | **blocked** | **ready** |
| 3 | 4.2 Перевод десятичной ↔ обыкновенной | 24–29 | «Ключевые факты» I–II на стр. 29 | ready (склейки KF_*) | **ready** |
| 4 | 6.1 Проценты | 111–113 | «Один процент — одна сотая…», \(1\%=1/100=0,01\) | ready (из rules[]) | **ready** |

Все страницы: `pdfOffset = 0` (печатный номер = номер PDF-страницы), источник файлов —
кэш `ocr/ocr_pages_math_aldamuratova_5grade_part2` + сверка PNG.

## Пилот 4.5: реконструкция vs учебник

Реконструкция `RC_4501` была **по смыслу верна**, но **не дословна**:

| | Текст |
|--|-------|
| Учебник (рамка p.45–46) | «При умножении десятичной дроби на натуральное число сначала надо выполнить умножение, не обращая внимания на запятую. Затем в значении произведения отделить запятой справа столько цифр (десятичных знаков), сколько их имеется после запятой в десятичной дроби.» |
| `RC_4501` | «Чтобы умножить десятичную дробь на натуральное число, надо умножить её на это число, не обращая внимания на запятую, а в полученном произведении отделить запятой справа столько цифр, сколько их было после запятой в десятичной дроби.» |

`RC_4502` (нули слева/справа) в учебнике **отдельной рамкой не сформулирован** — это следствие
разобранного примера `0,009 · 8 = 0,072` на стр. 45, не второе правило.

Пилотную папку `content/runs/math-5-umnozhenie-…-20260804-1400/` **не переписывали**.

# Журнал работы агента: Қазақ тілі, 2 сынып, 2-бөлік (Кайырбекова и др.)

**Исходный файл:** `Қазақ тілі Кайырбекова А.И. учебник для 2 класса Часть 2.pdf`  
**Дата:** 2026-05-03  

---

## 1. Подготовка файла

- В каталоге `stud` PDF с кириллическим именем; для стабильных путей в скриптах создана копия: `kaz_tili_2_kaiyrbekova_part2.pdf`.
- Размер файла после копирования: ~5,9 МБ.

---

## 2. Текстовый слой PDF

- Запуск `extract-pdfjs.mjs` (pdfjs `getTextContent`): **112 страниц**, как у 1-й части.
- На выборочных страницах (1, 100, 110) в тексте только водяной знак OKULYK — **полноценного текстового слоя нет**, как в части 1.
- Вывод: без OCR содержание не извлечь.

---

## 3. OCR (пайплайн как в части 1)

- Инструменты: `tools/pdf-extract/ocr-pdfjs.mjs`, рендер **pdfjs-dist** + **@napi-rs/canvas**, OCR **tesseract.js** (`kaz+rus`), масштаб рендера **2**.
- Каталог вывода (по правилу skill, всё под `ocr/`):  
  `ocr/ocr_pages_kaz_tili_2_kaiyrbekova_part2/`
- Обработаны страницы: **1–8** (метаданные, обложка, вводный фрагмент) и **95–112** (хвост учебника: уроки, словарь, грамматика, оглавление, выходные данные).
- Время прогона на этой машине: порядка **1,5–2 минут** на указанный набор (предупреждения pdfjs про Helvetica — ожидаемо, рендер всё равно пригоден для OCR).

---

## 4. Отличия структуры от части 1 (важно)

По OCR **не** повторяется схема «словарь ≈ 100, грамматика ≈ 106, оглавление ≈ 110»:

| Блок | PDF-страница (2-бөлік) | Комментарий |
|------|-------------------------|---------------|
| Қазақша-орысша сөздік | **104** начало, **105–106** продолжение | На 100 стр. идёт **урок** (Түркістан), не словарь. |
| Грамматикалық анықтамалар | **107–110** | Части речи и т.д. на **107**, дальше септік/жіктік/көптік, на **110** — сөйлем түрлері және меншікті/шақтық мағына. |
| МАЗМҰНЫ | **111** | Оглавление; цифры и номера уроков сильно зашумлены OCR. |
| Фрагмент оглавления раздела 8 | **112** (выходные данные) | Дублирует список сабақтар «Саяхат» с более читаемыми номерами страниц — использовано для **уточнения** названий уроков блока 8. |

Эти отличия отражены в `source.quality_report` итогового JSON.

---

## 5. Словарь (парсинг в JSON)

- Проблемы OCR: одна строка совмещала заголовок **«5. …»** и первую пару **kk — ru**; заголовок **«6. …»** был разорван между двумя строками вместе с «ӘДЕБИЕТІ».
- В `build-kaz-json-part2.mjs` добавлены точечные **нормализации текста** (`fixPart2GlossaryText`) и **построчный** разбор разделов, чтобы не сливались бөлімдер **5 / 6 / 7 / 8**.
- Итог: **4 секции** словаря, **138** пар в `glossary_kk_ru.sections` (часть строк с двумя колонками в одной строке OCR даёт неточные `ru` — отмечено как medium/low в отчёте качества).

---

## 6. Темы сабақтар және `page_start`

- Список **50** тем: бөлімдер **5–8** (денсаулық, салт және ауыз әдебиеті, қоршаған орта, саяхат) с нумерацией сабақов по оглавлению (стр. 111) и уточнением блока 8 по стр. 112.
- **`page_start`**: восстановлены **равномерной сеткой** (шаг 2, старт с 4), по аналогии с 1-й частью, с поправкой на границы бөлімдер (например, бөлім 7 с ~62, бөлім 8 с ~84). Без полного OCR уроков 4–95 это **низкая уверенность** — рекомендована сверка с печатным МАЗМҰНЫ.

---

## 7. Правила и `topic_rule_links`

- Те же стабильные `id` правил, что в части 1 (`r_zat_esim`, `r_septik_zhalgau`, …), но **`source_pdf_pages`** сдвинуты на **107–110**.
- Для каждого из **50** сабақов добавлена запись в **`topic_rule_links`** (`topic_id` вида `p2_u501` … `p2_u810`): `rule_ids`, `relation` (`primary` / `related` / `review`), краткий **`note_ru`** (педагогическая привязка, не дословный текст учебника).

---

## 8. Выходные артефакты

| Артефакт | Путь |
|----------|------|
| JSON v2 | `kaz_tili_kaiyrbekova_2grade_part2_parsed_v2.json` |
| Сборка JSON | `tools/pdf-extract/build-kaz-json-part2.mjs` |
| OCR тексты/картинки | `ocr/ocr_pages_kaz_tili_2_kaiyrbekova_part2/` (в `.gitignore`) |
| Копия PDF (латиница) | `kaz_tili_2_kaiyrbekova_part2.pdf` |
| Этот журнал | `kaz_tili_kaiyrbekova_2grade_part2_agent_log.md` |

---

## 9. Метаданные (из OCR стр. 2)

- Издательство: Атамұра, Алматы, **2022**.
- ISBN (по OCR): **978-601-10-0055-0** (часть 2), общий **978-601-10-0034-5** — последний символ в скане иногда «ломается»; при публикации сверить с титулом бумажной книги.

---

## 10. Что сделать дальше (после OCR 4–103)

1. При необходимости поднять **scale** рендера (например, 3) для «трудных» страниц и перезаписать только их.
2. Вручную проверить **2–3** сабақа, где заголовок в OCR сильно отличается от формулировки в JSON.

---

## 11. Дополнение: полный OCR уроков 4–103 (2026-05-03, вторая сессия)

- Запущена команда:  
  `node ocr-pdfjs.mjs … kaz_tili_2_kaiyrbekova_part2.pdf …/ocr_pages_kaz_tili_2_kaiyrbekova_part2 "4-103" 2`
- Обработано **100** PDF-страниц; время ~**3,7 мин** на этой машине. Файлы дописаны в тот же каталог `ocr/ocr_pages_kaz_tili_2_kaiyrbekova_part2/` (png + txt по страницам).
- В **`build-kaz-json-part2.mjs`** добавлено:
  - **`refineTopicsFromOcr`**: для каждого сабақа поиск ключевых фраз в «окне» вокруг исходной сеточной страницы (`grid ±` несколько страниц), чтобы не цеплять одноимённые фрагменты из другого места книги. Для четырёх уроков **«Сен не үйрендің?»** заданы фиксированные страницы (**32, 60, 82, 102**).
  - Доп. ключи OCR для спорных тем: **p2_u604** (Бата / жақсы тілек), **p2_u708** (Екі лақ), **p2_u709** (в т.ч. «Кустар мен балыктар»), **p2_u710** («Суды үнемдейік»).
  - Исправлены названия в данных: **«Бата — жақсы тілек»**, **«Екі лақ»** (по OCR учебника).
  - В **`example_tasks_v2.verbatim_examples`** вырезаны фрагменты с **«1-тапсырма»** с страниц **4, 12, 34, 62, 84, 100** (6 примеров, `confidence: medium_ocr`).
- В **`kaz_tili_kaiyrbekova_2grade_part2_parsed_v2.json`** у тем появилось поле **`page_start_ref`**: `ocr_title_scan` | `grid_fallback` | `ocr_review_lesson`.
- В `source.extraction_method` добавлено поле **`ocr_lesson_page_range`: `"4-103"`**.

---

*Конец журнала (обновлено после OCR 4–103).*
